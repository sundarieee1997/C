#include<stdio.h>
int main()
{
	int count_words=0,i;
	char str[20];
	printf("enter the string");
	scanf("%s",str);
	if(str[i]=='str')
	{
		count_words++;
	}
	printf("\n number of words in string :%d",count_words);
	return(0);
}
